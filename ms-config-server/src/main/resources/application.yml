server:
  port: 7070

spring:
  application:
    name: ms-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/markmercado1/ms-actividad.git # repositorio
          searchPaths:
            - config-data
            - config-data-empleo
          default-label: main

  security:
    user:
      name: root
      password: 123456
